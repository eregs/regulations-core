language: python
sudo: false
python:
  - "2.7"
  - "3.4"
  - "3.5"
  - "pypy"
install:
  - pip install coveralls tox-travis
script:
  - tox
after_success:
  - if [[ $INTEGRATION_TARGET = '' ]] && [[ $TRAVIS_PYTHON_VERSION = '3.5' ]]; then coveralls; fi
