[buildout]
parts = python django
develop = .
eggs = regcore

[python]
recipe = zc.recipe.egg
interpreter = python
eggs = ${buildout:eggs}

[django]
recipe = djangorecipe
project = regcore
projectegg = regcore
settings = example_settings
test = regcore regcore_read regcore_write
eggs = ${buildout:eggs}
       anyjson
       django-nose
       jsonschema
       mock
       pyelasticsearch
       south
